export {
  css,
  customEvent,
  define,
  element,
  on,
  onChange,
  onDisconnect,
  onPropChange,
  useInternals,
} from "./element.ts";

export type { CustomElement, PropTypesProps, TypedShadow } from "./element.ts";
